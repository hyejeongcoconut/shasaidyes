<div id=search-results data-controller="search">
  <!--<%= simple_form_for :search, url: vendors_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <%= text_field_tag :query,
          params[:query],
          class: "form-control string required",
          placeholder: "Find a vendor"
        %>
      <button name="button" type="submit" class="btn btn-flat" data-action="click->search#refresh">
        <i class="fas fa-search"></i> Search
      </button>
    </div>
  <% end %>-->
  <div class="search-form-control form-group">
            <input class="form-control string required" type="text" name="search[query]" id="search_query" />
            <button class="btn btn-flat" data-action="click->search#refresh">
              <i class="fas fa-search"></i> Search
            </button>
          </div>
  <h1>Vendors Index</h1>
  <div data-target="search.result" id="search_div">
    <% @vendors.each do |vendor| %>
      <p>Name: <%= vendor.name  %>, Category: <%= vendor.category  %>, City: <%= vendor.city  %></p>
    <% end %>
  </div>
</div>
